plugins {
    id 'java'
}

group = 'org.mangorage'
version = 'unspecified'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation platform('org.junit:junit-bom:5.10.0')
    testImplementation 'org.junit.jupiter:junit-jupiter'

    implementation(rootProject.project(":Core"))
    implementation(rootProject.project(":ExamplePlugin2"))
}

test {
    useJUnitPlatform()
}